name: Fam Space Threads

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Mode d'exécution"
        required: true
        type: choice
        default: dry-run
        options:
          - dry-run
          - live-one
          - live-all
      target_slug:
        description: "Slug uniquement pour live-one"
        required: false
        type: string
  schedule:
    - cron: "37 8 * * *"

permissions:
  contents: write

concurrency:
  group: fam-space-threads
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Recuperer le bridge
        uses: actions/checkout@v4
        with:
          ref: main
          path: bridge
          fetch-depth: 0

      - name: Recuperer Fam Space production
        uses: actions/checkout@v4
        with:
          repository: DevWeb13/fam-space-qwik
          ref: production
          token: ${{ secrets.FAM_SPACE_TOKEN }}
          path: fam-space
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Verifier le publisher Threads
        shell: bash
        run: node --check bridge/scripts/publish-threads.mjs

      - name: Executer le publisher Threads
        shell: bash
        env:
          FAM_SPACE_DIR: ${{ github.workspace }}/fam-space
          PINTEREST_FEED_URL: https://www.fam-space.fr/pinterest-v2.xml
          THREADS_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.mode || 'live-all' }}
          THREADS_TARGET_SLUG: ${{ inputs.target_slug }}
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
        run: node bridge/scripts/publish-threads.mjs bridge/threads-state.json

      - name: Enregistrer l'etat Threads
        if: ${{ always() }}
        shell: bash
        run: |
          set -euo pipefail
          cd bridge
          if git diff --quiet -- threads-state.json; then
            echo "Aucun changement d'état Threads à enregistrer."
            exit 0
          fi
          git config user.name "fam-space-actions"
          git config user.email "actions@users.noreply.github.com"
          git add threads-state.json
          git commit -m "state: record Threads publications"
          git push origin HEAD:main
